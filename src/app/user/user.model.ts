// type User = {
//   id: string;
//   avatar: string;
//   name: string;
// };
// with the "type" keyword, you can define other types


export interface User {
    id: string;
    avatar: string;
    name: string;
  };
  // with the "interface" keyword, you can only define object types